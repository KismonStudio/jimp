{
  "name": "jimp-language",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "compile": "node compiler/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  }
}
